# The dependency tree is committed and reviewed through package-lock.json.
save-exact=true
package-lock=true

# Do not execute install-time lifecycle scripts from dependencies.
ignore-scripts=true

# Require the public registry over TLS.
registry=https://registry.npmjs.org/
strict-ssl=true

# Avoid unrelated output during controlled installs.
fund=false
